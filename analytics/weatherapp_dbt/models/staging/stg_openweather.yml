version: 2

models:
  - name: stg_openweather
    description: Parsed and typed staging view over RAW.RAW_OPENWEATHER JSON.
    config:
      materialized: view
      tags:
        - weather
    columns:
      - name: location_id
        tests: [not_null]
      - name: event_date
        tests: [not_null]